apiVersion: v1
kind: Service
metadata:
  name: cert-exporter
  namespace: monitoring
  labels:
    app: cert-exporter
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
  - name: cert-exporter
    port: 8000
    targetPort: 8000
  selector:
    app: cert-exporter
