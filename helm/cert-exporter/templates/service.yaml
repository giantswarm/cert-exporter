apiVersion: v1
kind: Service
metadata:
  name: cert-exporter
  namespace: {{ .Release.Namespace }}
  labels:
    app: cert-exporter
    giantswarm.io/monitoring: "true"
spec:
  ports:
  - name: cert-exporter
    port: 9005
    targetPort: cert-exporter
  selector:
    app: cert-exporter
